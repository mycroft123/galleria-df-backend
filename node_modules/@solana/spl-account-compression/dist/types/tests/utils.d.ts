import { AnchorProvider } from '@coral-xyz/anchor';
import { Keypair, Signer, TransactionInstruction } from '@solana/web3.js';
import { ValidDepthSizePair } from '../src';
import { MerkleTree } from '../src/merkle-tree';
export declare function confirmAndLogTx(provider: AnchorProvider, txId: string, verbose?: boolean): Promise<void>;
export declare function execute(provider: AnchorProvider, instructions: TransactionInstruction[], signers: Signer[], skipPreflight?: boolean, verbose?: boolean): Promise<string>;
export declare function createTreeOnChain(provider: AnchorProvider, payer: Keypair, numLeaves: number, depthSizePair: ValidDepthSizePair, canopyDepth?: number): Promise<[Keypair, MerkleTree]>;
export declare function createEmptyTreeOnChain(provider: AnchorProvider, payer: Keypair, depthSizePair: ValidDepthSizePair, canopyDepth?: number): Promise<Keypair>;
export type PrepareTreeArgs = {
    canopyDepth: number;
    depthSizePair: ValidDepthSizePair;
    payer: Keypair;
    provider: AnchorProvider;
};
export declare function prepareTree(args: PrepareTreeArgs): Promise<Keypair>;
//# sourceMappingURL=utils.d.ts.map